apiVersion: apps/v1
kind: Deployment
metadata:
  generation: 1
  labels:
    app: webserver
  name: webserver
  namespace: default
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: webserver
  template:
    metadata:
      labels:
        app: webserver
    spec:
      containers:
      - image: nginx:1.25.2
        imagePullPolicy: IfNotPresent
        name: nginx
        ports:
        - containerPort: 80
